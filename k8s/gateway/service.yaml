apiVersion: v1
kind: Service

metadata:
  name: gateway-service
  namespace: smart-crowd

spec:
  selector:
    app: gateway

  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000

  type: ClusterIP