global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "express-gateway"
    metrics_path: /metrics

    static_configs:
      - targets:
          - express-gateway:3000