# Model Artifacts

This directory stores the trained model artifacts and `metadata.json` generated by:

```bash
python scripts/train_models.py
```

The service loads these files on startup:

- `crowd_density_model.joblib`
- `risk_classifier_model.joblib`
- `anomaly_detector_model.joblib`
- `metadata.json`

